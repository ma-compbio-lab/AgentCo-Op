models:
  default_executor:
    provider: mock
    model: mock-llm
    temperature: 0.0
    max_tokens: 4096
  high_reasoning:
    provider: mock
    model: mock-llm
    temperature: 0.0
    max_tokens: 8192

runtime:
  max_graph_patches: 3
  max_node_retries: 2
  max_cost_usd: 2.0
  max_wall_time_s: 900
