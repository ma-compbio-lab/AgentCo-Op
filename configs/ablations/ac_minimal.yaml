# Ablation method config (`ablation.md` §3) — AC-Minimal.
method: ac_minimal
ablation_variant: AC-Minimal
skills_tools: false
gate_repair: false
yaml_implementation:
  variant_name: AC-Minimal
  defines: configs/benchmarks/_base.yaml::variants:AC-Minimal
  applied_knobs:
    disable_gates: true
    disable_reviewer: true
notes: |
  Combines the AC-NoGate and AC-NoSkillsTools knobs; carries the
  same proxy caveat as AC-NoSkillsTools (tool / sandbox nodes are
  still active).
