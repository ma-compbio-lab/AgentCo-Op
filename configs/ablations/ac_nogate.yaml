# Ablation method config (`docs/experiments/ablation.md` §3) — AC-NoGate.
method: ac_nogate
ablation_variant: AC-NoGate
skills_tools: true
gate_repair: false
yaml_implementation:
  variant_name: AC-NoGate
  defines: configs/benchmarks/_base.yaml::variants:AC-NoGate
  applied_knobs:
    disable_gates: true
notes: |
  Skill-driven topology + tool/sandbox nodes still active; every gate
  policy is forced to `max_activations: 0` (gates fire 0 times). Logs
  "would-trigger" decisions in the trace so we can later estimate
  rescue rate against AC-Full.
