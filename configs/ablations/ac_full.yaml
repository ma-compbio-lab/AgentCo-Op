# Ablation method config (`ablation.md` §3) — AC-Full.
# This is a documentation-only YAML: the actual switches are applied by
# `agentcoop run-benchmark -v AC-Full` reading variants from
# `configs/benchmarks/_base.yaml`. Kept here so users can audit the
# 2 × 2 factorial design at a glance.
method: ac_full
ablation_variant: AC-Full
skills_tools: true
gate_repair: true
yaml_implementation:
  variant_name: AC-Full
  defines: configs/benchmarks/_base.yaml::variants:AC-Full
  applied_knobs: []  # no overrides — uses every default in the compiled blueprint
notes: |
  Equivalent to the existing AC-Gated variant. Reuse the Sessions 4–6
  AC-Gated full-split numbers as the AC-Full row of the ablation
  table to save compute.
