# @package _global_

defaults:
  - scanpy_moignard15_method_transfer_case

benchmark:
  planner_mode: fixed
  enable_refinement_subgraph: false

blueprint:
  budget:
    max_total_usd: 8.0
    max_total_tokens: 25000
    max_wall_time_s: 3600
    max_tool_calls: 3
    max_container_starts: 2
