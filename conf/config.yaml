defaults:
  - _self_

task:
  goal: "Describe the risk factors in the plan."
  constraints: []
  success_criteria: []
  budget_tokens: 8000
  input_modalities:
    - text
  output_modalities:
    - text

method: orchestrated

model:
  backend: openai
  name: gpt-4o-mini
  planner: null
  worker: null
  judge: null
  aggregator: null
  api_key: ${oc.env:OPENAI_API_KEY,null}

log_dir: logs

hydra:
  job:
    chdir: false
  output_subdir: null
  run:
    dir: .
