defaults:
  - _self_

tasks: "data/tasks.jsonl"
method: orchestrated
log_dir: logs
max_samples: null

model:
  backend: openai
  name: gpt-4o-mini
  planner: null
  worker: null
  judge: null
  aggregator: null
  api_key: ${oc.env:OPENAI_API_KEY,null}

hydra:
  job:
    chdir: false
  output_subdir: null
  run:
    dir: .
