__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
# Run outputs. Nothing under here is a deliverable; `agentcoop run --out` and
# `agentcoop bench run --out` write self-contained JSON that can be regenerated.
runs/
# Raw datasets, kept on disk but never in VCS.
data/*
!data/.gitkeep
!data/data_hashes.json
!data/README.md
# External repositories, cloned for reference.
external/
.secrets/
