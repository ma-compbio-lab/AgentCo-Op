[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "agentcoop"
version = "0.1.0"
description = "AgentCo-Op: a task-conditioned workflow compiler for multi-agent systems."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "AgentCo-Op authors" }]
dependencies = [
  "pydantic>=2",
  "typer>=0.12",
  "rich>=13",
  "networkx>=3",
  "jinja2>=3",
  "pyyaml>=6",
  "jsonschema>=4",
  "tenacity>=8",
  "httpx>=0.27",
  "python-dotenv>=1",
]

[project.optional-dependencies]
bench = [
  "datasets>=2.20",
  "evaluate>=0.4",
  "numpy>=1.26",
  "pandas>=2.2",
  "scikit-learn>=1.5",
]
repo = [
  "docker>=7",
  "gitpython>=3",
]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
]

[project.scripts]
agentcoop = "agentcoop.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["agentcoop"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
